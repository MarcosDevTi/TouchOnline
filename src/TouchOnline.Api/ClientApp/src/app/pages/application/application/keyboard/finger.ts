export class Finger {
    constructor(codigos: number[], nomeDedo: string, handCode: string) {
        this.Codigos = codigos;
        this.NomeDedo = nomeDedo;
        this.HandCode = handCode;
    }
    Codigos: number[];
    NomeDedo: string;
    HandCode: string;
}
